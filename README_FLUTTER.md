# تطبيق Flutter - منصة SmartJudi القضائية

## نظرة عامة

تطبيق Flutter متكامل للاتصال مع Django backend لإدارة النظام القضائي. التطبيق يدعم جميع الأدوار (قاضي، محامي، مواطن، مدير، كاتب عدل) ويوفر واجهات مستخدم حديثة وسهلة الاستخدام.

## الميزات

### ✅ المكتملة

- **المصادقة**: تسجيل الدخول باستخدام JWT مع حفظ تلقائي للـ tokens
- **الشاشة الرئيسية**: Dashboard ديناميكي حسب دور المستخدم
- **إدارة الدعاوى**: 
  - عرض قائمة الدعاوى مع pagination
  - إضافة دعوى جديدة
  - تعديل دعوى موجودة
  - حذف دعوى
  - Pull to refresh
- **إعدادات CORS**: تم إعداد Django للسماح بالاتصال من Flutter

### 🚧 قيد التطوير

- إدارة الأطراف (المدعون/المدعى عليهم)
- إدارة الجلسات
- إدارة الأحكام
- رفع المرفقات
- البحث والفلترة المتقدمة
- الإشعارات

## البنية

```
lib/
├── config/
│   └── api_config.dart          # إعدادات API و endpoints
├── models/
│   ├── user_model.dart          # نموذج المستخدم
│   └── lawsuit_model.dart       # نموذج الدعوى
├── services/
│   └── api_service.dart         # خدمة HTTP للاتصال مع Django
├── providers/
│   ├── auth_provider.dart       # State management للمصادقة
│   └── lawsuit_provider.dart    # State management للدعاوى
├── screens/
│   ├── login_screen.dart        # شاشة تسجيل الدخول
│   ├── home_screen.dart         # الشاشة الرئيسية (Dashboard)
│   ├── lawsuits_list_screen.dart # قائمة الدعاوى
│   └── lawsuit_detail_screen.dart # تفاصيل/إضافة/تعديل دعوى
└── main.dart                    # نقطة البداية
```

## الحزم المستخدمة

- `http`: للاتصال مع Django API
- `provider`: لإدارة الحالة (State Management)
- `shared_preferences`: لحفظ tokens محلياً
- `intl`: لتنسيق التواريخ
- `jwt_decoder`: لفك تشفير JWT tokens
- `file_picker`: لاختيار الملفات (للمستقبل)
- `image_picker`: لاختيار الصور (للمستقبل)

## الإعداد والتشغيل

### 1. تثبيت الحزم

```bash
flutter pub get
```

### 2. إعداد الاتصال مع Django

افتح `lib/config/api_config.dart` وعدّل `baseUrl`:

```dart
// Android Emulator
static const String baseUrl = 'http://10.0.2.2:8000';

// iOS Simulator
static const String baseUrl = 'http://localhost:8000';

// جهاز فعلي (استبدل بـ IP جهازك)
static const String baseUrl = 'http://192.168.1.100:8000';
```

### 3. تشغيل Django

```bash
cd smartju
python manage.py runserver
```

### 4. تثبيت django-cors-headers (إذا لم يكن مثبتاً)

```bash
cd smartju
pip install django-cors-headers
```

### 5. تشغيل Flutter

```bash
flutter run
```

## الأدوار والصلاحيات

### قاضي (Judge)
- عرض جميع الدعاوى في محكمته
- إدارة الجلسات
- إصدار الأحكام
- عرض الطعون

### محامي (Lawyer)
- إنشاء دعاوى جديدة
- تعديل الدعاوى الخاصة به
- إضافة الأطراف
- رفع المرفقات
- إضافة المذكرات والردود

### مواطن (Citizen)
- عرض دعاويه فقط
- متابعة حالة الدعوى
- عرض الجلسات والأحكام

### مدير (Admin)
- جميع الصلاحيات
- إدارة المستخدمين
- إدارة المحاكم
- عرض سجل الإجراءات

### كاتب عدل (Notary)
- عرض الدعاوى
- إضافة الوثائق

## API Endpoints المستخدمة

- `POST /api/token/` - تسجيل الدخول
- `POST /api/token/refresh/` - تحديث Token
- `GET /api/profiles/me/` - الملف الشخصي الحالي
- `GET /api/lawsuits/` - قائمة الدعاوى
- `POST /api/lawsuits/` - إنشاء دعوى
- `GET /api/lawsuits/{id}/` - تفاصيل دعوى
- `PATCH /api/lawsuits/{id}/` - تحديث دعوى
- `DELETE /api/lawsuits/{id}/` - حذف دعوى

## التصميم

- **Material Design 3**: استخدام أحدث معايير Material Design
- **RTL Support**: دعم كامل للغة العربية
- **Responsive**: تصميم متجاوب يعمل على جميع الأحجام
- **Dark Mode Ready**: جاهز لدعم الوضع الليلي (يمكن إضافته)

## استكشاف الأخطاء

### مشكلة الاتصال

1. تأكد من أن Django يعمل على `http://localhost:8000`
2. تحقق من إعدادات CORS في `smartju/smartju/settings/base.py`
3. للجهاز الفعلي، تأكد من أن IP صحيح وأن الجهاز والكمبيوتر على نفس الشبكة

### مشكلة المصادقة

1. تحقق من أن username/password صحيحين
2. تأكد من أن UserProfile موجود في Django
3. جرب حذف التطبيق وإعادة تثبيته (لحذف tokens القديمة)

### مشكلة في العرض

1. تأكد من تثبيت جميع الحزم: `flutter pub get`
2. تحقق من عدم وجود أخطاء في console
3. جرب `flutter clean` ثم `flutter pub get`

## الخطوات التالية

1. **إضافة المزيد من الشاشات**:
   - شاشة الأطراف (Plaintiffs/Defendants)
   - شاشة الجلسات (Hearings)
   - شاشة الأحكام (Judgments)
   - شاشة الطعون (Appeals)

2. **تحسينات UI/UX**:
   - إضافة animations
   - تحسين الألوان والخطوط
   - إضافة loading states أفضل
   - إضافة empty states

3. **ميزات إضافية**:
   - البحث والفلترة
   - الإشعارات
   - رفع المرفقات
   - عرض الملفات

4. **الأمان**:
   - تشفير البيانات المحلية
   - Certificate pinning
   - Biometric authentication

## المساهمة

لإضافة ميزات جديدة أو إصلاح أخطاء، يرجى:
1. إنشاء branch جديد
2. إضافة التغييرات
3. اختبار التغييرات
4. إنشاء Pull Request

## الترخيص

هذا المشروع مخصص للاستخدام الحكومي القضائي.

